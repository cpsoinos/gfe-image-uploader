export interface LocationInfo {
  city: string
  region?: string
  countryCode: string
}

export interface WorkplaceInfo {
  title: string
  companyName: string
}

export interface ImageTransformations {
  x: number
  y: number
  width: number
  height: number
}
